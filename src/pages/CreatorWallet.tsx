import React, { useEffect, useState } from 'react';
import { motion } from 'motion/react';
import { Wallet, Clock, CheckCircle2, DollarSign } from 'lucide-react';
import CreatorSidebar from '../components/CreatorSidebar';
import CreatorTopBar from '../components/CreatorTopBar';
import BindWalletButton from '../components/BindWalletButton';
import { useCreatorProfile } from '../hooks/useCreatorProfile';
import { useCampaigns } from '../hooks/useCampaigns';

const appleEase = [0.16, 1, 0.3, 1];

export default function CreatorWallet() {
  const { profile } = useCreatorProfile();
  const { getCreatorActiveCampaigns } = useCampaigns();
  const [participations, setParticipations] = useState<any[]>([]);
  useEffect(() => { getCreatorActiveCampaigns().then(setParticipations).catch(() => setParticipations([])); }, [getCreatorActiveCampaigns]);
  const totalEarned = Number(profile?.total_earned || 0);
  const transactions = participations.filter(p => ['approved', 'paid'].includes(p.status)).map(p => ({ id: p.id, date: p.approved_at || p.joined_at, campaign: p.campaign?.title || 'Campaign', amount: Number(p.calculated_reward || 0), status: p.status === 'paid' ? 'Released' : 'Approved' }));
  return <div className="min-h-screen bg-[#0A0A1E] text-[#F5F5F7] font-sans selection:bg-cyan/30 flex"><CreatorSidebar /><CreatorTopBar /><main className="flex-1 md:ml-64 mt-20 p-4 md:p-8"><div className="max-w-6xl mx-auto space-y-8"><div className="flex flex-col md:flex-row md:items-end justify-between gap-6"><div><motion.h1 initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.8, ease: appleEase }} className="text-3xl font-semibold tracking-tight text-white flex items-center gap-3"><Wallet className="w-8 h-8 text-cyan" /> Wallet</motion.h1><p className="text-muted mt-2">Track your earnings and manage your connected wallet.</p></div><div className="glass-panel rounded-2xl p-4 border border-white/10 flex flex-col gap-2 items-center"><div className="flex items-center gap-3 mb-2"><div className="w-10 h-10 rounded-full bg-cyan/10 flex items-center justify-center shrink-0"><Wallet className="w-5 h-5 text-cyan" /></div><div><h3 className="text-sm font-medium text-white">Bound Wallet</h3><p className="text-xs text-muted">Permanent Address</p></div></div><BindWalletButton /></div></div><div className="grid grid-cols-1 gap-6"><div className="glass-panel rounded-[2rem] p-8 border border-white/10"><div className="flex items-center gap-3 mb-4"><div className="w-10 h-10 rounded-xl bg-cyan/10 flex items-center justify-center"><DollarSign className="w-5 h-5 text-cyan" /></div><h2 className="text-lg font-medium text-white">Total Earned</h2></div><div className="text-5xl font-bold text-white tracking-tight mb-2">${totalEarned.toLocaleString()} <span className="text-xl text-muted font-medium">USDC</span></div><p className="text-sm text-muted mt-8">Total USDC earned from campaigns.</p></div></div><div className="glass-panel rounded-[2rem] border border-white/10 overflow-hidden"><div className="p-6 border-b border-white/10"><h2 className="text-xl font-semibold text-white">Transaction History</h2></div>{transactions.length === 0 ? <div className="p-8 text-muted text-center">No creator payout transactions yet.</div> : <div className="overflow-x-auto"><table className="w-full text-left border-collapse"><thead><tr className="border-b border-white/10 bg-white/5"><th className="py-4 px-6 text-xs font-semibold text-muted uppercase tracking-wider">Date</th><th className="py-4 px-6 text-xs font-semibold text-muted uppercase tracking-wider">Campaign</th><th className="py-4 px-6 text-xs font-semibold text-muted uppercase tracking-wider text-right">Amount</th><th className="py-4 px-6 text-xs font-semibold text-muted uppercase tracking-wider text-right">Status</th></tr></thead><tbody className="divide-y divide-white/5">{transactions.map(tx => <tr key={tx.id} className="hover:bg-white/5"><td className="py-4 px-6 text-sm text-muted">{new Date(tx.date).toLocaleDateString()}</td><td className="py-4 px-6 font-medium text-white">{tx.campaign}</td><td className="py-4 px-6 text-right font-semibold text-cyan">+${tx.amount.toLocaleString()}</td><td className="py-4 px-6 text-right"><span className="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-md border text-xs font-medium bg-green-500/10 border-green-500/20 text-green-400">{tx.status === 'Released' ? <CheckCircle2 className="w-3.5 h-3.5" /> : <Clock className="w-3.5 h-3.5" />}{tx.status}</span></td></tr>)}</tbody></table></div>}</div></div></main></div>;
}
